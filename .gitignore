# Claude Code — memory is local-only (backed up via cadence-private)
.claude/memory

# Claude Code — local settings
.claude/settings.local.json

# Claude Code — push gate flag files
.claude/.push-authorized*
